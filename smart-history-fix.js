(() => {
  const badge = document.querySelector('.smart-search-badge');
  if (!badge) return;
  badge.textContent = 'GPT-5.6 LUNA ACTIVE · TRACKER EVIDENCE';
  badge.title = 'Natural-language search uses GPT-5.6 Luna first, with local tracker reasoning only as a fallback.';
})();
