# Job Search Tracker

Simple Vercel dashboard for applications and speculative outreach.

## Updating the tracker

Edit `data/applications.csv` and commit the change to `main`. The Vercel project redeploys from GitHub.

The CSV is the single data source used by the page. Keep the existing column names and add one row for each application or speculative email.
